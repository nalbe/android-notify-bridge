# Gradle / Android build
.gradle/
build/
local.properties
*.apk

# release/ holds ONLY the tracked distribution APK; anything else the
# build tooling drops in there (output-metadata.json, extra copies) stays
# ignored.
release/*
!release/notifybridge-release.apk

# Kotlin
.kotlin/

# Build artifacts
*.zip
*.rar

# OS junk
Thumbs.db
Desktop.ini
.DS_Store